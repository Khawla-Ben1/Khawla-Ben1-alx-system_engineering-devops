#!/usr/bin/env bash
# script to run whoami as another user

if [ -z "$1" ]; then
  echo "Usage: $0 <username>"
  exit 1
fi
sudo -u "$1" whoami 2>/dev/null || echo "Error: User '$1' does not exist or sudo is not configured properly."
